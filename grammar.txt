expr
        : KEYWORD:VAR IDENTIFIER EQ expr
        : term ((PLUS|MINUS) term)*

term
        : factor ((MUL|DIV|QEDIV|MOD) factor)*

factor
        : (PLUS|MINUS) factor
        : power

power
        : atom (POW factor)*

atom
        : NUM|IDENTIFIER
        : LPAREN expr RPAREN
